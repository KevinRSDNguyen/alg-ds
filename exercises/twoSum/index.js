// --- Directions
// Given an array of numbers, return all pairs that add up to a given sum.
// The numbers can be used more than once. Use a hash map!
// --- Examples:
// twoSum([1, 2, 2, 3, 4], 4) --> [[2, 2], [3, 1]]

function twoSum(array, sum) {}

module.exports = twoSum;
